- # CyberAssistant PRO 🛡️ v3.7.2

  Инструмент для автоматизированной разведки (Recon), поиска уязвимостей (CVE) и генерации профессиональных отчетов. Создан для пентестеров и специалистов по кибербезопасности.

  ## 🚀 Основные возможности

  - **Интеллектуальный Recon**: Комбинирует данные Shodan (пассивная разведка) и Nmap (активное сканирование) в одном окне.
  - **Mentor Advice (База знаний)**: Анализирует 25 критических портов и выдает советы по эксплуатации и команды «Next Step».
  - **NIST CVE Lookup**: Мгновенное получение описания уязвимости напрямую из базы NIST NVD API.
  - **Умные отчеты**: Автоматическое сохранение результатов в красивые `.md` файлы с последующим **авто-открытием** в системе.
  - **Локальный перевод**: Команда `tr` переводит последний вывод (например, описание CVE) на русский язык.

  ## 🛠 Требования и установка

  ### Предварительные условия

  1. **Python 3.10+**
  2. **Nmap**: Должен быть установлен в системе (путь настраивается в `main.py`).
  3. **API Shodan**: Необходим ключ с [shodan.io](https://account.shodan.io).

  ### Установка зависимостей

  Bash

  ```
  pip install shodan python-nmap requests deep-translator beautifulsoup4
  ```

  ## 📖 Инструкция по эксплуатации

  Запустите скрипт: `python main.py`. Вводите команды прямо в терминал:

  - **Сканирование IP**: Просто введите IP (например, `8.8.8.8`). Скрипт соберет данные Shodan, просканирует порты через Nmap и даст советы.
  - **Поиск CVE**: Введите номер уязвимости (например, `CVE-2019-0708`). Вы получите описание и прямую ссылку на NIST.
  - **Перевод**: Введите `tr` сразу после получения данных на английском.
  - **Отчеты**: После сканирования нажмите `y`, чтобы создать файл. Он откроется автоматически.

  ## 🗂 Структура проекта

  - `main.py` — основной код терминала.
  - `reports/` — папка, где хранятся сгенерированные отчеты (создается автоматически).
  - `requirements.txt` — список необходимых библиотек.

  ------

  ## 🌍 Образовательный контекст

  Инструмент поддерживает изучение пентестинга через практические советы (Mentor Advice). Локальная база портов охватывает ключевые сервисы: от стандартных HTTP/SSH до баз данных (Redis, MongoDB, ElasticSearch) и систем управления Windows (MSRPC, SMB).